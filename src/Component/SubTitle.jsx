import React from 'react';


const SubTitle = ({children}) => {
    return (
       <p  className="text-lg md:text-xl text-gray-600 text-center leading-relaxed mb-10 ">{children}</p>
    );
};

export default SubTitle;